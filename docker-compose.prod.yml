services:
  traefik:
    image: traefik:v3.1
    restart: always
    command:
      - "--providers.file.filename=/etc/traefik/dynamic.yml"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--entrypoints.web.http.redirections.entrypoint.to=websecure"
      - "--entrypoints.web.http.redirections.entrypoint.scheme=https"
      - "--certificatesresolvers.le.acme.email=${ACME_EMAIL}"
      - "--certificatesresolvers.le.acme.storage=/letsencrypt/acme.json"
      - "--certificatesresolvers.le.acme.httpchallenge=true"
      - "--certificatesresolvers.le.acme.httpchallenge.entrypoint=web"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./traefik/dynamic.yml:/etc/traefik/dynamic.yml:ro
      - letsencrypt:/letsencrypt

  app:
    build: .
    restart: always
    env_file: .env.prod
    depends_on:
      postgis:
        condition: service_healthy
      redis:
        condition: service_started
      rabbitmq:
        condition: service_started
    expose:
      - "8000"
    volumes:
      - uploads:/app/uploads
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  notification-worker:
    build: .
    restart: always
    env_file: .env.prod
    depends_on:
      postgis:
        condition: service_healthy
      redis:
        condition: service_started
      rabbitmq:
        condition: service_started
    command: python -m app.workers.notification_worker

  fanout-worker:
    build: .
    restart: always
    env_file: .env.prod
    depends_on:
      postgis:
        condition: service_healthy
      redis:
        condition: service_started
      rabbitmq:
        condition: service_started
    command: python -m app.workers.fanout_worker

  # 데모용 세일 주기 생성·재생성. 짝수 정각(00·02·…·22 KST) 2시간 간격 + 기동 시 1회.
  sale-generator:
    build: .
    restart: always
    env_file: .env.prod
    depends_on:
      postgis:
        condition: service_healthy
    command: python -m app.workers.sale_generator_worker

  # 예약 자동 만료(5분)·픽업 노쇼 자동 환불(30분) 보장 배치.
  expiry-worker:
    build: .
    restart: always
    env_file: .env.prod
    depends_on:
      postgis:
        condition: service_healthy
      redis:
        condition: service_started
    command: python -m app.workers.expiry_worker

  # Redis 키 만료 이벤트로 데드라인 정각 만료/환불(즉시 계층).
  expiry-listener:
    build: .
    restart: always
    env_file: .env.prod
    depends_on:
      postgis:
        condition: service_healthy
      redis:
        condition: service_started
    command: python -m app.workers.expiry_listener

  postgis:
    image: postgis/postgis:16-3.4
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7
    restart: always
    volumes:
      - redisdata:/data

  rabbitmq:
    image: rabbitmq:3.13-management
    restart: always
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD}
    volumes:
      - rabbitmqdata:/var/lib/rabbitmq

volumes:
  pgdata:
  redisdata:
  rabbitmqdata:
  uploads:
  letsencrypt:
